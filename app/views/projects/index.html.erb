<div class="container">
  <div class="row">
    <div class="col s12">
      <ul class="collection with-header">
        <li class="collection-header"><h4>Projects</h4></li>
        <% @projects.each do |p| %>
          <%= link_to p.name, project_path(p), class: "collection-item" %>
        <% end %>
      </ul>
    </div>
  </div>
</div>
